<!-- Target for toggling the sidebar `.sidebar-checkbox` is for regular
     styles, `#sidebar-checkbox` for behavior. -->
<input type="checkbox" class="sidebar-checkbox" id="sidebar-checkbox">

<!-- Toggleable sidebar -->
<div class="sidebar" id="sidebar">
  <div class="sidebar-item">
    <p>Random notes, lectures and workshops during my adventures on geospatial technologies. Have fun!</p>
  </div>

  <nav class="sidebar-nav">
    <a class="sidebar-nav-item {{ if eq .Url "/" }} active {{ end }}" href="/">Home</a>
    <a class="sidebar-nav-item {{ if eq .Url "post" }} active {{ end }}" href="/post">Archives</a>
    <a class="sidebar-nav-item {{ if eq .Url "lectures" }} active {{ end }}" href="/lectures">Lectures</a>

    {{ $thisperma := .Permalink }}
    {{ range .Site.Recent.ByWeight }}
      {{ if isset .Params "sidebar" }}
        <a class="sidebar-nav-item {{ if eq .Permalink $thisperma }} active {{ end }}" href="{{ .Permalink }}">{{ .Title }}</a>
      {{ end }}
    {{ end }}

    <!-- I (dany) don't really need this 
    <a class="sidebar-nav-item" href="{{ .Site.Params.Github.Url }}/archive/{{ .Site.Params.Github.Head }}.zip">Download</a>
    <a class="sidebar-nav-item" href="{{ .Site.Params.Github.Url }}">GitHub project</a>
    <span class="sidebar-nav-item">Currently on {{ .Site.Params.Github.Head }}</span> 
     -->
  </nav>

  <div class="sidebar-item">
    <p>&copy; {{ .Site.LastChange.Year }}. Visit <a href="https://github.com/danylaksono" target="_blank">my github page</a> for more.</p>
  </div>
</div>
